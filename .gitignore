# Secrets - these MUST never be committed
.env
.env.*
!.env.example
service-account.json
*-service-account*.json
serviceAccountKey*.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
venv/
.venv/
env/

# Tooling / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# Local recordings - linked from YouTube instead. Git keeps binaries forever,
# so a 40 MB video would bloat every future clone permanently.
*.mp4
*.mov
*.mkv
*.webm
Screenshot*.png

# OneDrive folder metadata
desktop.ini
